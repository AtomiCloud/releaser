# A Go stub is built per target and the shim swaps the prebuilt Bun binary in (prebuilt builder
# is Pro-only, mirrors sulfone.iridium); GoReleaser owns archives, deb/rpm, cask, and the release.
version: 2

project_name: releaser

builds:
  - id: bun
    main: ./goreleaser.go
    binary: releaser
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    # Only darwin arm64 ships (no intel mac).
    ignore:
      - goos: darwin
        goarch: amd64
    hooks:
      post:
        - cmd: ./scripts/release/goreleaser-shim.sh "{{ .Path }}" "{{ .Os }}" "{{ .Arch }}"

archives:
  - id: archive
    ids: [bun]
    formats: [tar.gz]
    name_template: '{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}'

checksum:
  name_template: 'checksums.txt'

release:
  # install.sh is served from the GitHub release (see INSTALLATION.md's one-line installer).
  extra_files:
    - glob: scripts/release/install.sh

nfpms:
  - id: packages
    ids: [bun]
    package_name: releaser
    formats: [deb, rpm]
    maintainer: AtomiCloud <ops@atomi.cloud>
    description: Offline-first conventional release and commit-lint automation
    license: MIT
    bindir: /usr/bin

# GoReleaser uses casks (not the deprecated `brews`) for pre-compiled binaries.
homebrew_casks:
  - name: releaser
    ids: [archive]
    binaries: [releaser]
    repository:
      owner: AtomiCloud
      name: homebrew-tap
      token: '{{ .Env.HOMEBREW_TAP_TOKEN }}'
    homepage: https://github.com/AtomiCloud/releaser
    description: Offline-first conventional release and commit-lint automation
    # The Bun binary ships unsigned; strip the macOS quarantine attribute on install.
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/releaser"]
          end
