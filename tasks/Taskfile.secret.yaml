# ### workspace-secret
# #### source: workspace
version: '3'

tasks:
  fetch:
    desc: Fetch the selected Infisical environment into .env
    cmds:
      - ./scripts/local/secrets.sh fetch

  scan:
    desc: Scan tracked content for secrets
    cmds:
      - ./scripts/local/secrets.sh scan
